
import Foundation

enum EventType: String {
    case launchApp = "launch_app"
    case leftApp = "left_app"
    case afterIdfa = "after_idfa"
    case userSubscribed = "user_subscribed"
    case userPurchase = "user_purchase"
}
